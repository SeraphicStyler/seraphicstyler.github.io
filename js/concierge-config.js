// Set only after the separate concierge Worker has been deployed and verified.
// Empty keeps the published-answer guide local; it never pretends a model is connected.
export const guideEndpoint = '';
